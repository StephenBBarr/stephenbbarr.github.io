{
  "name": "stephen-barr-portfolio",
  "private": true,
  "scripts": {
    "start": "npm run build -- --blog-dir ../gemini-lab/content/blog && python3 -m http.server 8000 --bind 127.0.0.1 --directory dist",
    "build": "node scripts/build.cjs",
    "check:release": "node scripts/check-release.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "linkedom": "0.18.13"
  }
}
