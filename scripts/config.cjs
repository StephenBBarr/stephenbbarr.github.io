"use strict";

// The builder reads the published index and puts each post's fetch address in its page.
module.exports = {
  enabled: true,
  bridgeBase: "https://bridge.stoathoughts.com/",
  capsuleIndex: "gemini://gemini.stoathoughts.com/blog/",
};
